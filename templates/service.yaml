apiVersion: v1
kind: Service
metadata:
  name: {{ include "nextcloud-spreed.fullname" . }}
  labels:
    {{- include "nextcloud-spreed.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nextcloud-spreed.selectorLabels" . | nindent 4 }}
